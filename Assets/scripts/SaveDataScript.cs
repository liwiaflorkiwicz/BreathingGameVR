using UnityEngine;

public class SaveDataScript : MonoBehaviour
{
    // Pobierac dane z kontrolerów i zapisywac je jako wartosci
    // Przesylac je na serwer jako JSON
    // Potem te dane analizowac i porownac z inputami z SliderInteractions
    // Wyswietlic wyniki w EndGamePanel

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
